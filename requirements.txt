# SecureSG dependencies — SP1 (Foundation + Audit Spine) scope.
# Later build cycles append their own dependencies with a documented reason.

# Runtime
pydantic>=2.6
pydantic-settings>=2.2
aiosqlite>=0.20
pyyaml>=6.0

# Runtime — SP3 (GuardFormer semantic layer). Both are imported lazily, so the
# package and the full non-gated test suite import and run without them present.
llama-cpp-python>=0.2.90   # local Qwen3-0.6B Q4_K_M GGUF inference (CPU)
huggingface-hub>=0.24      # one-time weights download (scripts/fetch_model.py)

# Runtime — SP4 (Warden embeddings). Lazy-imported and gated like the SP3 model;
# the non-gated test suite runs on a stub and needs neither.
sentence-transformers>=3.0  # all-MiniLM-L6-v2 sentence embeddings (CPU)
torch>=2.2                  # sentence-transformers backend

# Runtime — SP5 (Proxy). The transparent proxy is an HTTP service; httpx is the
# async client used to forward approved calls to the MCP backend (and to drive
# the ASGI app in tests). Not gated — required to run and test the proxy.
httpx>=0.27                 # async MCP backend client + ASGI test transport
fastapi>=0.110             # JSON-RPC proxy app (session control + RPC data plane)
uvicorn[standard]>=0.30    # ASGI server that runs the proxy (main.py)

# Test tooling
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=4.1
hypothesis>=6.100

# Lint + type-check (strict typing is a project requirement)
ruff>=0.6
mypy>=1.11
