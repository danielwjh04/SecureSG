# SecureSG dependencies — SP1 (Foundation + Audit Spine) scope.
# Later build cycles append their own dependencies with a documented reason.

# Runtime
pydantic>=2.6
pydantic-settings>=2.2
aiosqlite>=0.20
pyyaml>=6.0
openai>=1.40                # async OpenAI client: the guard judge + Warden embeddings

# Runtime — optional ML wheels (in-process MiniLM embeddings, an offline
# alternative to the default OpenAI embedding backend) live in
# requirements-ml.txt. They are lazy-imported and gated: this package and the
# full non-gated test suite import and run without them, so the default install
# and CI stay lean. Install them only for the offline embedding backend.

# Runtime — SP5 (Proxy). The transparent proxy is an HTTP service; httpx is the
# async client used to forward approved calls to the MCP backend (and to drive
# the ASGI app in tests). Not gated — required to run and test the proxy.
httpx>=0.27                 # async MCP backend client + ASGI test transport
fastapi>=0.110             # JSON-RPC proxy app (session control + RPC data plane)
uvicorn[standard]>=0.30    # ASGI server that runs the proxy (main.py)

# Test tooling
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=4.1
hypothesis>=6.100

# Lint + type-check (strict typing is a project requirement)
ruff>=0.6
mypy>=1.11
types-PyYAML>=6.0  # stubs for pyyaml so mypy --strict types policy/anchor loads
