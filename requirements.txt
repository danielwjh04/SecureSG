# SecureSG dependencies — SP1 (Foundation + Audit Spine) scope.
# Later build cycles append their own dependencies with a documented reason.

# Runtime
pydantic>=2.6
pydantic-settings>=2.2
aiosqlite>=0.20
pyyaml>=6.0

# Runtime — SP3 (GuardFormer semantic layer). Both are imported lazily, so the
# package and the full non-gated test suite import and run without them present.
llama-cpp-python>=0.2.90   # local Qwen3-0.6B Q4_K_M GGUF inference (CPU)
huggingface-hub>=0.24      # one-time weights download (scripts/fetch_model.py)

# Test tooling
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=4.1
hypothesis>=6.100

# Lint + type-check (strict typing is a project requirement)
ruff>=0.6
mypy>=1.11
