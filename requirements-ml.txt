# SecureSG — optional ML wheels (opt-in, offline embedding backend only).
#
# The guard judge and the default embedding backend run on OpenAI (in the base
# requirements). These wheels power one optional alternative: in-process MiniLM
# intent-drift embeddings, for running the Warden embedding layer offline with no
# OpenAI calls. They are heavy native wheels (a large torch download), imported
# lazily behind the swappable embedding seam, so neither the package nor the
# non-gated test suite needs them. Install on top of the base requirements only
# when using the offline embedding backend:
#
#     pip install -r requirements.txt -r requirements-ml.txt
#
# then set SECURESG_EMBEDDING_PROVIDER=sentence-transformers.

# Warden embeddings — SP4 (intent-to-action drift detection), offline backend.
sentence-transformers>=3.0  # all-MiniLM-L6-v2 sentence embeddings (CPU)
torch>=2.2                  # sentence-transformers backend
