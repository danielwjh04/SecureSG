# SecureAI scanner SPA local variables.
#
# The standalone scanner Worker has been retired. Runtime secrets and scanner
# policy now belong to `secureai/.dev.vars` and Cloudflare Worker secrets.
# Keep this file only for Vite client-side values if the SPA needs one later.
