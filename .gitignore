# Virtual environments
.venv/
venv/

# Python caches
__pycache__/
*.py[cod]
*.egg-info/

# Secrets and local config
.env

# SQLite audit databases (never committed)
*.db
*.db-wal
*.db-shm

# Test / coverage artifacts
.pytest_cache/
.coverage
coverage.xml
htmlcov/

# Model weights (never committed)
*.gguf
models/*.gguf
/model_weights/

# Local-only working notes, scratch, and office documents (never committed)
/.local/
.tmp-installer-*/
*.docx
iv.log

# Graphify knowledge-graph output (lives in Obsidian; never pushed to GitHub)
graphify-out/
graphify/
.graphify_detect.json

# Local Obsidian vault config (repo opened as a vault; personal, never pushed)
.obsidian/
